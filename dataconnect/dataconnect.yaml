specVersion: "v1beta"
serviceId: "Join-Kanban-Firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Join-Kanban-Firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
